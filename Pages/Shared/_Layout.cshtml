@using Microsoft.AspNetCore.Identity
@using SaillingLoc.Models
@using SaillingLoc.Services
@using SaillingLoc.Data
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager
@inject INotificationService NotificationService

@{
    var isSignedIn = SignInManager.IsSignedIn(User);
    User currentUser = null;
    IList<string> roles = new List<string>();
    int unreadNotificationsCount = 0;

    if (isSignedIn)
    {
        currentUser = await UserManager.GetUserAsync(User);
        roles = await UserManager.GetRolesAsync(currentUser);

        if (currentUser != null)
        {
            unreadNotificationsCount = await NotificationService.GetUnreadNotificationsCountAsync(currentUser.Id);
        }
    }
}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - SaillingLoc</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Icônes -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
<!-- Inclure jQuery, Popper.js et Bootstrap -->
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
@* <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script> *@
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <!-- Styles personnalisés -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SaillingLoc.styles.css" asp-append-version="true" />

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet" />

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
            color: #2d3e50;
        }

        /* --- DARK MODE --- */
        body.dark-mode {
            background-color: #121212 !important;
            color: #f1f1f1 !important;
        }

        body.dark-mode header,
        body.dark-mode footer,
        body.dark-mode .cta-section {
            background-color: #1f1f1f !important;
            color: #f1f1f1 !important;
        }

        body.dark-mode .nav-link {
            color: #f1f1f1 !important;
        }

        body.dark-mode .nav-link:hover {
            color: #AD7C59 !important;
        }

        body.dark-mode .navbar-brand {
            color: #fff !important;
        }

        /* --- HEADER --- */
        header {
            background-color: #F5F1EB;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
        }

        .navbar {
            padding: 0.8rem 0;
            background-color: transparent !important;
            border-bottom: none !important;
            box-shadow: none !important;
            margin-bottom: 0 !important;
        }

        .navbar-brand {
            font-weight: 700;
            color: #2d3e50 !important;
            padding: 0;
            margin-right: 50px;
        }

        .navbar-brand img {
            height: 45px;
        }

        /* --- NAV LINKS VISIBLES --- */
        .navbar-nav .nav-link {
            color: #2d3e50 !important;
            font-weight: 500;
            font-size: 0.95rem;
            padding: 0.5rem 1rem !important;
            border-radius: 6px;
            transition: all 0.2s ease;
        }

        .navbar-nav .nav-link:hover {
            color: #AD7C59 !important;
            background-color: rgba(173, 124, 89, 0.08);
        }

        /* --- BOUTONS --- */
        .custom-btn {
            background-color: transparent;
            color: #AD7C59;
            border: 1.5px solid #AD7C59;
            padding: 0.4rem 1.2rem;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .custom-btn:hover {
            background-color: #AD7C59;
            color: white;
        }

        .btn-outline-success {
            background-color: #AD7C59;
            color: white;
            border: 1.5px solid #AD7C59;
        }

        .btn-outline-success:hover {
            background-color: #9a6b4f;
            border-color: #9a6b4f;
        }

        /* --- SELECTEUR DE LANGUE --- */
        .language-selector {
            background-color: white;
            border: 1.5px solid #ddd;
            color: #2d3e50;
            padding: 0.4rem 0.8rem;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.9rem;
        }

        .language-selector:hover {
            border-color: #AD7C59;
            background-color: #fafafa;
        }

        /* --- USER INFO --- */
        .user-info {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #2d3e50;
            font-weight: 500;
            font-size: 0.9rem;
        }

        /* --- BADGES --- */
        .position-relative .badge {
            font-size: 0.65rem;
            padding: 0.2em 0.4em;
        }

        /* --- DARK MODE TOGGLE --- */
        .form-check-input:checked {
            background-color: #4B6A88;
            border-color: #4B6A88;
        }

        body.dark-mode .form-check-label {
            color: #f1f1f1 !important;
        }

        /* --- CTA + FOOTER --- */
        .cta-section {
            background-color: #fff;
            text-align: center;
            padding: 50px 20px;
            box-shadow: 0 -1px 6px rgba(0,0,0,0.1);
        }

        footer {
            background-color: #fff;
            border-top: 1px solid #dee2e6;
        }

        /* --- VISIBILITÉ MAXIMALE DES LIENS NAVBAR --- */
        header {
          position: relative;
          z-index: 9999 !important;
          background-color: #F5F1EB !important;
        }

        .navbar {
          background-color: #F5F1EB !important;
          z-index: 10000 !important;
        }

        .navbar-nav .nav-link {
          color: #000 !important;
          opacity: 1 !important;
          visibility: visible !important;
          position: relative;
          z-index: 10001;
        }

        .navbar-nav .nav-link:hover {
          color: #AD7C59 !important;
          background-color: rgba(173, 124, 89, 0.08);
        }

        /* ✅ Correction finale — rend visibles "Se connecter" et "Inscription" */
        .navbar .btn.custom-btn {
            background-color: #AD7C59 !important;
            color: #fff !important;
            border: 1.5px solid #AD7C59 !important;
            opacity: 1 !important;
            visibility: visible !important;
            position: relative;
            z-index: 10002 !important;
        }

        .navbar .btn.custom-btn:hover {
            background-color: #9a6b4f !important;
            border-color: #9a6b4f !important;
        }

        .navbar .btn.btn-outline-success {
            background-color: #2d3e50 !important;
            color: #fff !important;
            border: 1.5px solid #2d3e50 !important;
            opacity: 1 !important;
            visibility: visible !important;
            position: relative;
            z-index: 10003 !important;
        }

        .navbar .btn.btn-outline-success:hover {
            background-color: #1b2735 !important;
            border-color: #1b2735 !important;
        }


        .language-selector {
    display: inline-block !important;
    opacity: 1 !important;
    visibility: visible !important;
    position: relative !important;
    z-index: 10005 !important;
}


/* Centrer uniquement les liens principaux de la navbar */
.navbar-nav.me-auto {
    display: flex;
    justify-content: center; /* centre horizontalement */
    flex: 1; /* occupe tout l'espace disponible entre le logo et la droite */
    gap: 15px; /* espace entre les liens */
}

/* S'assurer que le logo reste à gauche et les boutons à droite */
.navbar-brand {
    margin-right: auto; /* pousse les liens vers le centre */
}

.navbar-nav.ms-auto {
    margin-left: auto; /* reste à droite */
}
 /* Assure que les éléments de droite restent visibles */
.navbar-nav.ms-auto > .nav-item {
    display: flex !important;
    align-items: center !important;
    opacity: 1 !important;
    visibility: visible !important;
    z-index: 10004 !important; /* plus haut que le reste */
}


    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" asp-area="" asp-page="/Index">
                    <img src="~/images/plan2.png" alt="Logo SailingLoc" />
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item"><a class="nav-link" asp-page="/Index">Accueil</a></li>

                        @if (isSignedIn)
                        {
                            <li class="nav-item"><a class="nav-link" asp-page="/Destinations">Destinations</a></li>
                            <li class="nav-item"><a class="nav-link" asp-page="/Categories">Categories</a></li>
                              @* <li class="nav-item"><a class="nav-link" asp-page="/MyReservations">Mes Réservations</a></li> *@
                                <li class="nav-item"><a class="nav-link" asp-page="/About">A propos</a></li>

                            @if (roles.Contains("Admin"))
                            {
                                <li class="nav-item"><a class="nav-link" asp-page="/AdminDashboard">Dashboard Admin</a></li>
                            }
<li class="nav-item">
    <a class="nav-link" asp-page="/Bateaux">Mes Bateaux</a>

</li>



                        }

                        <li class="nav-item"><a class="nav-link" asp-page="/Contacts">Contact</a></li>
                    </ul>

                    <ul class="navbar-nav ms-auto align-items-center">
                        <li class="nav-item me-3">
                            <select class="language-selector">
                                <option value="fr">FR</option>
                                <option value="en">EN</option>
                                <option value="es">ES</option>
                            </select>
                        </li>
@* 
                        <li class="nav-item me-3">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" role="switch" id="darkModeSwitch">
                                <label class="form-check-label" for="darkModeSwitch">🌙</label>
                            </div>
                        </li> *@

                        @if (isSignedIn)
                        {
                            <li class="nav-item me-3">@await Component.InvokeAsync("UnreadMessages")</li>
                            <li class="nav-item me-3">
                                <a href="/Notifications" class="nav-link position-relative p-2" title="Notifications">
                                    <i class="fa-regular fa-bell fa-lg"></i>
                                    @if (unreadNotificationsCount > 0)
                                    {
                                        <span class="badge bg-danger position-absolute top-0 start-100 translate-middle rounded-pill">
                                            @unreadNotificationsCount
                                        </span>
                                    }
                                </a>
                            </li>
                      <li class="nav-item dropdown me-3">
    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="fa-solid fa-user me-2"></i>
        <span>@currentUser.FirstName</span>
    </a>

    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
        <li>
            <a class="dropdown-item" asp-page="/Dashuser">
                <i class="fa-solid fa-user-circle me-2"></i> Mon espace
            </a>
        </li>

        <li><hr class="dropdown-divider"></li>

        <li>
            <form method="post" asp-area="Identity" asp-page="/Account/Logout" class="d-inline">
                <button type="submit" class="dropdown-item text-danger">
                    <i class="fa-solid fa-right-from-bracket me-2"></i> Déconnexion
                </button>
            </form>
        </li>
    </ul>
</li>


                        }
                        else
                        {
                            @* <li class="nav-item me-2"><a class="nav-link" asp-page="/Ajoutbat" style="font-weight: 600;">Enregistrer votre bateau</a></li> *@
                            <li class="nav-item me-2"><a class="btn btn-sm custom-btn" asp-page="/Account/Login">Se connecter</a></li>
                            @* <li class="nav-item"><a class="btn btn-sm btn-outline-success" asp-page="/Account/Register">Inscription</a></li> *@
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="pb-4">
        @RenderBody()
    </main>

    <!-- FOOTER -->
<footer style="background-color: #4B6A88; color: white; padding: 60px 0 0 0;">
    <div class="container">
        <div class="row">
            
            <!-- Colonne 1 : SAILINGLOC -->
            <div class="col-md-4 mb-4">
                <h5 style="font-weight: bold; margin-bottom: 20px; font-size: 18px;">SAILINGLOC</h5>
                <p style="color: #E0E7EF; font-size: 14px; line-height: 1.6; margin-bottom: 25px;">
                    Suivez-nous sur les réseaux sociaux et plongez dans l'univers SailingLoc. Partagez vos expériences, découvrez nos meilleures destinations et restez informé des nouveautés.
                </p>
                <div class="d-flex gap-3">
                    <a href="#" style="width: 40px; height: 40px; border-radius: 50%; background-color: #5A7A98; color: white; display: flex; align-items: center; justify-content: center; text-decoration: none; transition: background-color 0.3s;">
                        <i class="fab fa-instagram"></i>
                    </a>
                    <a href="#" style="width: 40px; height: 40px; border-radius: 50%; background-color: #5A7A98; color: white; display: flex; align-items: center; justify-content: center; text-decoration: none; transition: background-color 0.3s;">
                        <i class="fab fa-facebook-f"></i>
                    </a>
                    <a href="#" style="width: 40px; height: 40px; border-radius: 50%; background-color: #5A7A98; color: white; display: flex; align-items: center; justify-content: center; text-decoration: none; transition: background-color 0.3s;">
                        <i class="fab fa-linkedin-in"></i>
                    </a>
                </div>
            </div>

            <!-- Colonne 2 : Liens Rapides -->
            <div class="col-md-4 mb-4">
                <h5 style="font-weight: bold; margin-bottom: 20px; font-size: 18px;">Liens Rapides</h5>
                <ul style="list-style: none; padding: 0; margin: 0;">
                    <li style="margin-bottom: 10px;">
                        <a asp-page="/About" style="color: #E0E7EF; text-decoration: none; font-size: 14px; transition: color 0.3s;">À Propos</a>
                    </li>
                    <li style="margin-bottom: 10px;">
                        <a asp-page="/CGU" style="color: #E0E7EF; text-decoration: none; font-size: 14px; transition: color 0.3s;">CGU</a>
                    </li>
                    <li style="margin-bottom: 10px;">
                        <a asp-page="/Legal" style="color: #E0E7EF; text-decoration: none; font-size: 14px; transition: color 0.3s;">Mentions Légales</a>
                    </li>
                    <li style="margin-bottom: 10px;">
                        <a asp-page="/CookiePolicy" style="color: #E0E7EF; text-decoration: none; font-size: 14px; transition: color 0.3s;">Politique des Cookies</a>
                    </li>
                    <li style="margin-bottom: 10px;">
                        <a asp-page="/PrivacyPolicy" style="color: #E0E7EF; text-decoration: none; font-size: 14px; transition: color 0.3s;">Politiques de Confidentialité</a>
                    </li>
                    <li style="margin-bottom: 10px;">
                        <a asp-page="/Sitemap" style="color: #E0E7EF; text-decoration: none; font-size: 14px; transition: color 0.3s;">Plan du site</a>
                    </li>
                    <li style="margin-bottom: 10px;">
                        <a asp-page="/Contact" style="color: #E0E7EF; text-decoration: none; font-size: 14px; transition: color 0.3s;">Contact</a>
                    </li>
                </ul>
            </div>

            <!-- Colonne 3 : Newsletter -->
            <div class="col-md-4 mb-4">
                <h5 style="font-weight: bold; margin-bottom: 20px; font-size: 18px;">Ne manquez aucune opportunité</h5>
                <p style="color: #E0E7EF; font-size: 14px; line-height: 1.6; margin-bottom: 20px;">
                    Inscrivez-vous à notre newsletter et recevez en avant-première les meilleures offres de location, conseils de navigation et actualités exclusives.
                </p>
                <div class="position-relative" style="max-width: 350px;">
                    <input type="email" placeholder="Entrez votre email" style="width: 100%; padding: 12px 45px 12px 15px; border: none; border-radius: 6px; background-color: #5A7A98; color: white; font-size: 14px;" />
                    <button type="submit" style="position: absolute; right: 5px; top: 50%; transform: translateY(-50%); background-color: #AD7C59; border: none; border-radius: 4px; width: 35px; height: 35px; display: flex; align-items: center; justify-content: center; cursor: pointer;">
                        <i class="fas fa-arrow-right" style="color: white; font-size: 14px;"></i>
                    </button>
                </div>
            </div>

        </div>
    </div>

    <!-- Ligne de copyright -->
    <div style="background-color: #3E5A75; padding: 20px 0; margin-top: 40px;">
        <div class="container">
            <p class="text-center mb-0" style="color: #B8C5D6; font-size: 14px;">
                ©2025 Pandawan. All rights reserved.
            </p>
        </div>
    </div>
</footer>

<!-- Font Awesome pour les icônes -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <!-- SCRIPTS -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <!-- Dark Mode -->
    <script>
        const darkModeSwitch = document.getElementById('darkModeSwitch');
        const body = document.body;
        if (localStorage.getItem('darkMode') === 'enabled') {
            body.classList.add('dark-mode');
            darkModeSwitch.checked = true;
        }
        darkModeSwitch.addEventListener('change', () => {
            if (darkModeSwitch.checked) {
                body.classList.add('dark-mode');
                localStorage.setItem('darkMode', 'enabled');
            } else {
                body.classList.remove('dark-mode');
                localStorage.setItem('darkMode', 'disabled');
            }
        });
    </script>

    <!-- Cookie Banner -->
    <div id="cookie-banner" style="position: fixed; bottom: 0; left: 0; width: 100%; background-color: #4B6A88; color: white; padding: 15px; text-align: center; display: flex; justify-content: space-between; align-items: center; z-index: 1000;">
        <span>
            Nous utilisons des cookies pour améliorer votre expérience sur notre site.
            <a href="/Legal" style="color: #AD7C59; text-decoration: underline;">En savoir plus</a>
        </span>
        <button id="accept-cookies" style="background-color: #AD7C59; color: white; border: none; padding: 10px 20px; border-radius: 5px; cursor: pointer;">Accepter</button>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const banner = document.getElementById('cookie-banner');
            const acceptBtn = document.getElementById('accept-cookies');
            if (localStorage.getItem('cookiesAccepted') === 'true') banner.style.display = 'none';
            acceptBtn.addEventListener('click', function () {
                localStorage.setItem('cookiesAccepted', 'true');
                banner.style.display = 'none';
            });
        });
    </script>

    @RenderSection("Scripts", required: false)
</body>
</html>
